<Page x:Class="PromtAiPdfPro.Views.SignPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="{DynamicResource Nav_Sign}">

    <Viewbox Stretch="Uniform" VerticalAlignment="Top" HorizontalAlignment="Center">
        <Grid Width="1150" Height="820" Margin="48,32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <ui:Button Grid.Column="0" Click="BtnBack_Click" Margin="0,0,16,0" Padding="8" VerticalAlignment="Center" ToolTip="{DynamicResource Common_Back}">
                    <ui:SymbolIcon Symbol="ArrowLeft24"/>
                </ui:Button>
                
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource Sign_Title}" Style="{StaticResource PremiumHeaderTitle}" FontSize="28"/>
                </StackPanel>

                <StackPanel x:Name="PanelPageNav" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Visibility="Collapsed">
                    <ui:Button Icon="ArrowLeft24" Click="BtnPrevPage_Click" Margin="0,0,4,0"/>
                    <TextBlock x:Name="TxtPageInfo" Text="Page 0 / 0" VerticalAlignment="Center" Width="100" TextAlignment="Center" FontWeight="Bold"/>
                    <ui:Button Icon="ArrowRight24" Click="BtnNextPage_Click" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="380"/>
                </Grid.ColumnDefinitions>

                <!-- Preview Area -->
                <Border Grid.Column="0" Background="#1A1A1A" CornerRadius="12" Margin="0,0,24,0" ClipToBounds="True">
                    <Grid x:Name="GridPdfPreview">
                        <StackPanel x:Name="StackPreviewEmpty" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="DocumentPdf24" FontSize="64" Opacity="0.2"/>
                            <TextBlock Text="{DynamicResource Msg_SelectPdfFirst}" Opacity="0.5" Margin="0,10,0,0"/>
                        </StackPanel>

                        <Image x:Name="ImgPdfPreview" Stretch="Uniform"/>
                        
                        <Canvas x:Name="CanvasOverlay" Background="Transparent" 
                                MouseLeftButtonDown="CanvasOverlay_MouseLeftButtonDown"
                                MouseMove="CanvasOverlay_MouseMove"
                                MouseLeftButtonUp="CanvasOverlay_MouseLeftButtonUp">
                            <Border x:Name="BorderSignPreview" BorderBrush="{DynamicResource PremiumAccentBrush}" BorderThickness="2" Background="#330078D4" Width="150" Visibility="Collapsed" Cursor="Hand">
                                <Image x:Name="ImgSignOnPdf" Stretch="Uniform"/>
                            </Border>
                        </Canvas>
                    </Grid>
                </Border>

                <!-- Controls -->
                <StackPanel Grid.Column="1">
                    <ui:Card Padding="20">
                        <StackPanel>
                            <ui:Button Content="{DynamicResource Common_Select}" Click="BtnSelectPdf_Click" Width="220" Appearance="Primary" Margin="0,0,0,12" HorizontalAlignment="Left"/>
                            <TextBlock x:Name="TxtSelectedPdf" Text="{DynamicResource Common_PathPlaceholder}" FontSize="11" Opacity="0.6" Margin="0,0,0,16" TextWrapping="Wrap"/>
                            
                            <TextBlock Text="{DynamicResource Sign_Method}" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <RadioButton x:Name="RbDraw" Content="{DynamicResource Sign_Draw}" IsChecked="True" Checked="RbSignMethod_Changed" Margin="0,0,16,0"/>
                                <RadioButton x:Name="RbUpload" Content="{DynamicResource Sign_Upload}" Checked="RbSignMethod_Changed"/>
                            </StackPanel>

                            <!-- Draw Panel -->
                            <StackPanel x:Name="PanelDraw">
                                <Border BorderBrush="#33FFFFFF" BorderThickness="1" Background="White" CornerRadius="8" Height="140" Margin="0,0,0,8">
                                    <InkCanvas x:Name="InkSign" Background="Transparent" StrokeCollected="InkSign_StrokeCollected"/>
                                </Border>
                                <ui:Button Content="{DynamicResource Sign_Clear}" Click="BtnClear_Click" Width="100" HorizontalAlignment="Right" Margin="0,0,0,16" Appearance="Secondary"/>
                            </StackPanel>

                            <!-- Upload Panel -->
                            <StackPanel x:Name="PanelUpload" Visibility="Collapsed">
                                <ui:Button Content="{DynamicResource Sign_Upload}" Click="BtnSelectImage_Click" Width="220" Margin="0,0,0,8" HorizontalAlignment="Left"/>
                                <Image x:Name="ImgUploaded" Height="100" Margin="0,0,0,16" Visibility="Collapsed" Stretch="Uniform"/>
                            </StackPanel>

                            <TextBlock Text="{DynamicResource Sign_ApplyTo}" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                            <RadioButton x:Name="RbAllPages" Content="{DynamicResource Sign_AllPages}" IsChecked="True" Margin="0,0,0,4"/>
                            <RadioButton x:Name="RbCurrent" Content="{DynamicResource Sign_CurrentPage}" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <RadioButton x:Name="RbCustom" Content="{DynamicResource Sign_CustomPages}" VerticalAlignment="Center"/>
                                <ui:TextBox x:Name="TxtCustomRange" Width="80" Margin="10,0,0,0" PlaceholderText="1, 3-5"/>
                            </StackPanel>

                            <TextBlock Text="{DynamicResource Sign_Size}" FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                            <Slider x:Name="SliderSize" Minimum="50" Maximum="400" Value="150" ValueChanged="Slider_ValueChanged" Margin="0,0,0,12"/>

                            <TextBlock Text="{DynamicResource Sign_Opacity}" FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                            <Slider x:Name="SliderOpacity" Minimum="0" Maximum="1" Value="1" TickFrequency="0.1" ValueChanged="Slider_ValueChanged" Margin="0,0,0,24"/>

                            <ui:Button Click="BtnSign_Click" Background="#28a745" Foreground="White" BorderThickness="0" CornerRadius="8" Width="220" Height="50" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Signature24" Margin="0,0,12,0" Filled="True" FontSize="20"/>
                                    <TextBlock Text="{DynamicResource Sign_Process}" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>
                            </ui:Button>
                        </StackPanel>
                    </ui:Card>
                </StackPanel>
            </Grid>
        </Grid>
    </Viewbox>
</Page>
