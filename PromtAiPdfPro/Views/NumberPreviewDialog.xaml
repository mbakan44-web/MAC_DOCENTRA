<ui:FluentWindow x:Name="DialogWindow"
                 x:Class="PromtAiPdfPro.Views.NumberPreviewDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="{DynamicResource Preview_WindowTitle}"
                 Height="700" Width="550"
                 WindowStartupLocation="CenterOwner"
                 Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="{DynamicResource Preview_Title}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="{DynamicResource Preview_Desc}" FontSize="14" Opacity="0.7" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- A4 Simulation (Scaled down to fit) -->
        <Border Grid.Row="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="424">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.2" ShadowDepth="10"/>
            </Border.Effect>
            <Canvas x:Name="PreviewCanvas" Width="300" Height="424" Background="White">
                <TextBlock x:Name="SampleNumber" Text="1" FontSize="12" Foreground="Black" FontWeight="Bold"/>
            </Canvas>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <TextBlock Text="{DynamicResource Preview_Ask}" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Content="{DynamicResource Preview_Yes}" Appearance="Primary" HorizontalAlignment="Stretch" Margin="0,0,8,0" Height="45" Click="BtnApprove_Click"/>
                <ui:Button Grid.Column="1" Content="{DynamicResource Preview_No}" Appearance="Secondary" HorizontalAlignment="Stretch" Margin="8,0,0,0" Height="45" Click="BtnCancel_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
